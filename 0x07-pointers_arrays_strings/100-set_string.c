#include "main.h"

/**
 * set_string - Entry point
 * @s: an input to the function
 * @to: an input to the function
 * Return: Always 0 (Success)
 */
void set_string(char **s, char *to)
{
	*s = to;
}
