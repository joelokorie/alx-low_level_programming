#include "main.h"
#include <stdio.h>

/**
  * main - The entry point. It prints name of the program
  * @argc: argument count keeps count of arguments
  * @argv: argument vector is an array of all arguments
  *
  * Return: Always zero
  */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
